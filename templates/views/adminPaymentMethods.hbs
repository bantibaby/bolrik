<!DOCTYPE html>
<html lang="en">
<head>
    {{>headlinks}}
    <title>Admin - Payment Methods</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f5f7fa;
            margin: 0;
            padding: 0;
        }
        
        .container {
            max-width: 1300px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #e1e5eb;
        }
        
        .page-title {
            font-size: 24px;
            font-weight: 600;
            color: #3a3a3a;
            margin: 0;
        }
        
        .card {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
            margin-bottom: 30px;
            overflow: hidden;
        }
        
        .card-header {
            background-color: #fff;
            padding: 20px 25px;
            border-bottom: 1px solid #edf2f9;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .card-header h2 {
            font-size: 18px;
            font-weight: 600;
            color: #3a3a3a;
            margin: 0;
        }
        
        .card-body {
            padding: 0;
        }
        
        .card-footer {
            background-color: #f9fafc;
            padding: 15px 25px;
            border-top: 1px solid #edf2f9;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .table-container {
            overflow-x: auto;
            width: 100%;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th {
            background-color: #f9fafc;
            color: #5e6e82;
            font-weight: 600;
            padding: 15px;
            text-align: left;
            border-bottom: 2px solid #edf2f9;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #edf2f9;
            color: #5e6e82;
            font-size: 14px;
        }
        
        tr:hover {
            background-color: #f9fafc;
        }
        
        .badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 600;
            margin-right: 5px;
            margin-bottom: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }
        
        .badge-primary {
            background-color: #4e73df;
            color: white;
        }
        
        .badge-warning {
            background-color: #f6c23e;
            color: #212529;
        }
        
        .badge-danger {
            background-color: #e74a3b;
            color: white;
        }
        
        .badge-success {
            background-color: #1cc88a;
            color: white;
        }
        
        .badge-secondary {
            background-color: #858796;
            color: white;
        }
        
        .badge-info {
            background-color: #36b9cc;
            color: white;
        }
        
        .btn {
            display: inline-block;
            font-weight: 600;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            user-select: none;
            border: 1px solid transparent;
            padding: 0.375rem 0.75rem;
            font-size: 14px;
            line-height: 1.5;
            border-radius: 0.35rem;
            transition: color 0.15s ease-in-out, background-color 0.15s ease-in-out, border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
            cursor: pointer;
            margin-left: 5px;
        }
        
        .btn-primary {
            color: #fff;
            background-color: #4e73df;
            border-color: #4e73df;
        }
        
        .btn-primary:hover {
            background-color: #2e59d9;
            border-color: #2653d4;
        }
        
        .btn-secondary {
            color: #fff;
            background-color: #858796;
            border-color: #858796;
        }
        
        .btn-secondary:hover {
            background-color: #717384;
            border-color: #6b6d7d;
        }
        
        .btn-danger {
            color: #fff;
            background-color: #e74a3b;
            border-color: #e74a3b;
        }
        
        .btn-danger:hover {
            background-color: #e02d1b;
            border-color: #d52a1a;
        }
        
        .filter-container {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
            padding: 0 25px;
        }
        
        .search-box {
            flex: 1;
            min-width: 250px;
            padding: 10px 15px;
            border: 1px solid #e1e5eb;
            border-radius: 5px;
            font-size: 14px;
            outline: none;
            transition: border-color 0.2s;
        }
        
        .search-box:focus {
            border-color: #4e73df;
            box-shadow: 0 0 0 0.2rem rgba(78, 115, 223, 0.25);
        }
        
        .filter-select {
            padding: 10px 15px;
            border: 1px solid #e1e5eb;
            border-radius: 5px;
            font-size: 14px;
            min-width: 200px;
            outline: none;
            transition: border-color 0.2s;
        }
        
        .filter-select:focus {
            border-color: #4e73df;
            box-shadow: 0 0 0 0.2rem rgba(78, 115, 223, 0.25);
        }
        
        .text-center {
            text-align: center;
        }
        
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            border-top-color: #4e73df;
            animation: spin 1s ease-in-out infinite;
            margin-right: 10px;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        .nav-buttons {
            display: flex;
            gap: 10px;
        }
        
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5);
            overflow: auto;
        }
        
        .modal-content {
            background-color: #fff;
            margin: 10% auto;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
            max-width: 500px;
            position: relative;
        }
        
        .close-modal {
            position: absolute;
            right: 20px;
            top: 15px;
            font-size: 28px;
            font-weight: bold;
            color: #aaa;
            cursor: pointer;
        }
        
        .close-modal:hover {
            color: #333;
        }
        
        .modal-title {
            margin-top: 0;
            color: #333;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
        }
        
        .modal-footer {
            margin-top: 20px;
            text-align: right;
            border-top: 1px solid #eee;
            padding-top: 15px;
        }
        
        @media (max-width: 768px) {
            .card-header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .card-header h2 {
                margin-bottom: 10px;
            }
            
            th, td {
                padding: 10px;
            }
            
            .nav-buttons {
                margin-top: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="page-header">
            <h1 class="page-title">Payment Methods Management</h1>
            <div class="nav-buttons">
                <a href="/admin/dashboard" class="btn btn-secondary">Dashboard</a>
                <a href="/admin/fingerprints" class="btn btn-secondary">Device Fingerprints</a>
                <a href="/admin/ip-addresses" class="btn btn-secondary">IP Monitoring</a>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <h2>Payment Methods</h2>
                <div>
                    <button id="refreshBtn" class="btn btn-primary">
                        <span id="loading-spinner" class="loading-spinner" style="display: none;"></span>
                        Refresh Data
                    </button>
                </div>
            </div>
            
            <div class="filter-container">
                <input type="text" id="searchInput" class="search-box" placeholder="Search payment methods...">
                <select id="filterSelect" class="filter-select">
                    <option value="all">All Payment Methods</option>
                    <option value="bank">Bank Accounts</option>
                    <option value="upi">UPI IDs</option>
                    <option value="paytm">Paytm Numbers</option>
                </select>
            </div>
            
            <div class="card-body">
                <div class="table-container">
                    <table id="paymentMethodsTable">
                        <thead>
                            <tr>
                                <th>Type</th>
                                <th>Details</th>
                                <th>User</th>
                                <th>Created</th>
                                <th>Last Used</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="paymentMethodsBody">
                            <tr>
                                <td colspan="7" class="text-center">
                                    <span class="loading-spinner"></span> Loading data...
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            
            <div class="card-footer">
                <div id="totalCount">Total: 0 payment methods</div>
            </div>
        </div>
    </div>
    
    <!-- Unlock Payment Method Modal -->
    <div id="unlockModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeModal('unlockModal')">&times;</span>
            <h3 class="modal-title">Unlock Payment Method</h3>
            <p>Are you sure you want to unlock this payment method? This will allow it to be used by other users.</p>
            <div>
                <label for="unlockReason">Reason for unlocking:</label>
                <input type="text" id="unlockReason" class="search-box" style="width: 100%; margin-top: 10px;" placeholder="Administrative action">
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" onclick="closeModal('unlockModal')">Cancel</button>
                <button class="btn btn-primary" id="confirmUnlockBtn">Unlock</button>
            </div>
        </div>
    </div>
    
    <!-- Lock Payment Method Modal -->
    <div id="lockModal" class="modal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeModal('lockModal')">&times;</span>
            <h3 class="modal-title">Lock Payment Method</h3>
            <p>Are you sure you want to lock this payment method? This will restrict it to be used only by the original user.</p>
            <div>
                <label for="lockReason">Reason for locking:</label>
                <input type="text" id="lockReason" class="search-box" style="width: 100%; margin-top: 10px;" placeholder="Security measure">
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary" onclick="closeModal('lockModal')">Cancel</button>
                <button class="btn btn-danger" id="confirmLockBtn">Lock</button>
            </div>
        </div>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            loadPaymentMethods();
            
            document.getElementById('refreshBtn').addEventListener('click', loadPaymentMethods);
            document.getElementById('searchInput').addEventListener('input', filterTable);
            document.getElementById('filterSelect').addEventListener('change', filterTable);
        });
        
        let currentPaymentMethodId = '';
        
        function loadPaymentMethods() {
            // Show loading spinner
            document.getElementById('loading-spinner').style.display = 'inline-block';
            document.getElementById('refreshBtn').disabled = true;
            
            fetch('/admin/payment-methods-data')
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        displayPaymentMethods(data.paymentMethods);
                        document.getElementById('totalCount').textContent = `Total: ${data.paymentMethods.length} payment methods`;
                    } else {
                        alert('Error loading payment methods: ' + data.message);
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('Failed to load payment methods. Please try again.');
                })
                .finally(() => {
                    // Hide loading spinner
                    document.getElementById('loading-spinner').style.display = 'none';
                    document.getElementById('refreshBtn').disabled = false;
                });
        }
        
        function displayPaymentMethods(paymentMethods) {
            const tbody = document.getElementById('paymentMethodsBody');
            tbody.innerHTML = '';
            
            if (paymentMethods.length === 0) {
                tbody.innerHTML = '<tr><td colspan="7" class="text-center">No payment methods found</td></tr>';
                return;
            }
            
            paymentMethods.forEach(pm => {
                const row = document.createElement('tr');
                row.dataset.id = pm._id;
                row.dataset.type = pm.type;
                
                // Type
                const typeCell = document.createElement('td');
                const typeBadge = document.createElement('span');
                typeBadge.className = 'badge badge-info';
                typeBadge.textContent = pm.type.toUpperCase();
                typeCell.appendChild(typeBadge);
                row.appendChild(typeCell);
                
                // Details
                const detailsCell = document.createElement('td');
                let detailsText = '';
                
                if (pm.type === 'bank') {
                    detailsText = `${pm.details.bankName || ''} - ${pm.details.accountNumber || ''} (${pm.details.ifsc || ''})`;
                } else if (pm.type === 'upi') {
                    detailsText = pm.details.upiId || '';
                } else if (pm.type === 'paytm') {
                    detailsText = pm.details.paytmNumber || '';
                }
                
                detailsCell.textContent = detailsText;
                row.appendChild(detailsCell);
                
                // User
                const userCell = document.createElement('td');
                if (pm.user) {
                    userCell.textContent = `${pm.user.fullname || 'Unknown'} (${pm.user.mobile || 'No mobile'})`;
                } else {
                    userCell.textContent = 'Unknown User';
                }
                row.appendChild(userCell);
                
                // Created
                const createdCell = document.createElement('td');
                createdCell.textContent = new Date(pm.createdAt).toLocaleString();
                row.appendChild(createdCell);
                
                // Last Used
                const lastUsedCell = document.createElement('td');
                lastUsedCell.textContent = pm.lastUsedAt ? new Date(pm.lastUsedAt).toLocaleString() : 'Never';
                row.appendChild(lastUsedCell);
                
                // Status
                const statusCell = document.createElement('td');
                const statusBadge = document.createElement('span');
                
                if (pm.isLocked) {
                    statusBadge.className = 'badge badge-success';
                    statusBadge.textContent = 'Locked';
                } else {
                    statusBadge.className = 'badge badge-warning';
                    statusBadge.textContent = 'Unlocked';
                }
                
                statusCell.appendChild(statusBadge);
                row.appendChild(statusCell);
                
                // Actions
                const actionsCell = document.createElement('td');
                
                if (pm.isLocked) {
                    const unlockBtn = document.createElement('button');
                    unlockBtn.className = 'btn btn-warning';
                    unlockBtn.textContent = 'Unlock';
                    unlockBtn.onclick = () => showUnlockModal(pm._id);
                    actionsCell.appendChild(unlockBtn);
                } else {
                    const lockBtn = document.createElement('button');
                    lockBtn.className = 'btn btn-danger';
                    lockBtn.textContent = 'Lock';
                    lockBtn.onclick = () => showLockModal(pm._id);
                    actionsCell.appendChild(lockBtn);
                }
                
                row.appendChild(actionsCell);
                
                tbody.appendChild(row);
            });
        }
        
        function showUnlockModal(paymentMethodId) {
            currentPaymentMethodId = paymentMethodId;
            document.getElementById('unlockModal').style.display = 'block';
            document.getElementById('unlockReason').value = 'Administrative action';
            
            document.getElementById('confirmUnlockBtn').onclick = function() {
                const reason = document.getElementById('unlockReason').value || 'Administrative action';
                togglePaymentMethodLock(currentPaymentMethodId, false, reason);
                closeModal('unlockModal');
            };
        }
        
        function showLockModal(paymentMethodId) {
            currentPaymentMethodId = paymentMethodId;
            document.getElementById('lockModal').style.display = 'block';
            document.getElementById('lockReason').value = 'Security measure';
            
            document.getElementById('confirmLockBtn').onclick = function() {
                const reason = document.getElementById('lockReason').value || 'Security measure';
                togglePaymentMethodLock(currentPaymentMethodId, true, reason);
                closeModal('lockModal');
            };
        }
        
        function closeModal(modalId) {
            document.getElementById(modalId).style.display = 'none';
        }
        
        function togglePaymentMethodLock(paymentMethodId, lock, reason) {
            // Show loading spinner
            document.getElementById('loading-spinner').style.display = 'inline-block';
            document.getElementById('refreshBtn').disabled = true;
            
            fetch('/admin/toggle-payment-method-lock', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ 
                    paymentMethodId, 
                    lock,
                    reason
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    loadPaymentMethods();
                } else {
                    alert('Error: ' + data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('Failed to update payment method status. Please try again.');
            })
            .finally(() => {
                // Hide loading spinner
                document.getElementById('loading-spinner').style.display = 'none';
                document.getElementById('refreshBtn').disabled = false;
            });
        }
        
        function filterTable() {
            const searchValue = document.getElementById('searchInput').value.toLowerCase();
            const filterValue = document.getElementById('filterSelect').value;
            const rows = document.querySelectorAll('#paymentMethodsBody tr');
            
            let visibleCount = 0;
            
            rows.forEach(row => {
                const type = row.dataset.type;
                const cells = row.querySelectorAll('td');
                let textContent = '';
                
                cells.forEach(cell => {
                    textContent += cell.textContent.toLowerCase() + ' ';
                });
                
                let showRow = textContent.includes(searchValue);
                
                if (filterValue !== 'all' && type !== filterValue) {
                    showRow = false;
                }
                
                row.style.display = showRow ? '' : 'none';
                
                if (showRow) {
                    visibleCount++;
                }
            });
            
            document.getElementById('totalCount').textContent = `Showing: ${visibleCount} payment methods`;
        }
        
        // Close the modals if user clicks outside of them
        window.onclick = function(event) {
            if (event.target.className === 'modal') {
                event.target.style.display = 'none';
            }
        }
    </script>
</body>
</html> 